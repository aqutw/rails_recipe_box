- @recipes.each_slice(3) do |rs|
  .row
    - rs.each do |r|
      .col-md-4
        .recipe
          .image_wrapper
            = link_to r do
              = image_tag r.image.url(:medium)
          %h2= link_to r.title, r